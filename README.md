<longcat_arg_key>path</longcat_arg_key>
<longcat_arg_value>/Users/mac/Documents/最强助手/github_bounty_hunter/workspaces/Xazz/README.md</longcat_arg_value>
<longcat_arg_key>old_string</longcat_arg_key>
<longcat_arg_value>**The Visual IDE** — full run receipt with SHA-256 code hash, real training loss and DP budget monitoring:

<div align="center">
<img src="docs/assets/ide_monitor.png" alt="Visual IDE monitor: Burn training loss, differential privacy budget (epsilon consumed), policy-as-code guardrail panel" width="94%">
</div>

---

## Architecture</longcat_arg_value>
<longcat_arg_key>new_string</longcat_arg_key>
<longcat_arg_value>**The Visual IDE** — full run receipt with SHA-256 code hash, real training loss and DP budget monitoring:

<div align="center">
<img src="docs/assets/ide_monitor.png" alt="Visual IDE monitor: Burn training loss, differential privacy budget (epsilon consumed), policy-as-code guardrail panel" width="94%">
</div>

---

## Check Diagnostics Gallery

`xazz check` catches four classes of errors at compile time — before any data is loaded or GPU time is spent. Each diagnostic pairs the `.xzz` snippet with the `line:col` output.

### 1. Did-you-mean — typo in column name